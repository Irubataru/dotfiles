# vi: set ft=yaml.ansible :
---

- hosts: flutterbox
  vars:
    modules:
      # Basics
      - system
      - basic
      - keyboard
      - zsh
      - fonts

      # Programming languages
      - go
      - npm
      - ruby
      - rust

      # Applications
      - X
      - alacritty
      - applications
      - awesome
      - bitwarden
      - clang
      - common
      - conky
      - git
      - gpg
      - latex
      - neovim
      - qutebrowser
      - tmux
      - utilities
      - vifm
  tasks:
    - include_role:
        name: "{{ item }}"
      loop: "{{ modules }}"
  roles:
    - ansible-modules-bitwarden
