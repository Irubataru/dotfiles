# vi: set ft=yaml.ansible :
---

- hosts: flutterbox
  vars:
    modules:
      # Basics
      - system
      - basic
      - keyboard
      - zsh
      - zoxide
      - fonts

      # Programming languages
      - clang
      - dotnet
      - lua
      - go
      - npm
      - ruby
      - rust

      # Applications
      - X
      - alacritty
      - applications
      - awesome
      - bitwarden
      - clang
      - common
      - conky
      - git
      - gpg
      - latex
      - navi
      - neovim
      - obsidian
      - qutebrowser
      - tldr
      - tmux
      - utilities
      - vifm
      - zathura
  tasks:
    - include_role:
        name: "{{ item }}"
      loop: "{{ modules }}"
  roles:
    - ansible-modules-bitwarden
