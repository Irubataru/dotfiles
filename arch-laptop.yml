---

- hosts: arch_laptop
  vars:
    roles:
      - basic
      - zsh
      - common
      - urxvt
      - tmux
      - X
      - vim
      - gpg
      - spacemacs
      - awesome
      - clang
      - conky
      - git
      - lastpass
      - latex
      - qutebrowser
      - ruby
      - scripts
      - go
      - fzf
      - npm
      - vifm
  tasks:
    - include_role:
        name: "{{ item }}"
      loop: "{{ roles }}"
