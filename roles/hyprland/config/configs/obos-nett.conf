# Performance optimizations
decoration {
    blur {
        enabled = false  # Blur is very expensive at this resolution
        size = 3
        passes = 1
    }
    inactive_opacity = 1.0  # Disable transparency
}

animations {
    enabled = no
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    animation = windows, 1, 3, myBezier
    animation = windowsOut, 1, 3, default, popin 80%
    animation = border, 1, 5, default
    animation = fade, 1, 3, default
    animation = workspaces, 1, 3, default
    # Reduce animation complexity
}

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    vfr = true  # Variable refresh rate can help
    vrr = 1  # Enable VRR if your monitor supports it
}

render {
  direct_scanout = 1
}

# Reduce damage tracking overhead
# general {
#     damage_tracking = full  # Try 'monitor' if still slow
# }
