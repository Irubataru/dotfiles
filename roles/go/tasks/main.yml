# vi: set ft=yaml.ansible :

- name: Install go (apt)
  ansible.builtin.apt:
    name: golang-go
    state: present
  become: true
  when:
    - install
    - package_manager == "apt"

- name: Fish configuration
  ansible.builtin.import_tasks: fish.yml
  when:
    - "'fish' in modules"
