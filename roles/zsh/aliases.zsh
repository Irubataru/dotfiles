alias reload!='. ~/.zshrc'

# Useful ls aliases
alias ls='ls --group-directories-first --color=auto --hide="*~"'
alias la='ls -a'
alias ll='ls -l'
alias lt='ls -t'
alias lla='ls -al'

# Verbose file operations
alias rm='rm -v'
alias mv='mv -v'
alias cp='cp -v'
