roles=(
{% for item in modules %}
  "{{ item }}"
{% endfor %}
)

#add each topic folder to fpath so that they can add functions and completion scripts
for role in ${roles}; do
  fpath=(${DOTFILES}/roles/${role} $fpath)
done
